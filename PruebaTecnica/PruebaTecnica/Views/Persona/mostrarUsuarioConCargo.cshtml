@model IEnumerable<PruebaTecnica.Models.Persona>

@{
    ViewBag.Title = "mostrarUsuarioConCargo";
}

<h2>mostrarUsuarioConCargo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cargo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cargo }) |
            @Html.ActionLink("Details", "Details", new { id=item.nombre }) |
        </td>
    </tr>
}

</table>
