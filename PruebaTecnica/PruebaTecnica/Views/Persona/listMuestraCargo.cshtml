@model IEnumerable<PruebaTecnica.Models.cargo>

@{
    ViewBag.Title = "listMuestraCargo";
}

<h2>listMuestraCargo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model=> model.car_cod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.car_des)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.car_cod)
        </td>
        <td>
           @* @Html.DisplayFor(modelItem => item.car_des)*@

            @Html.ActionLink(item.car_des,"usuarioXCargo", new {cargo =item.car_des} )
          

        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.car_cod }) |
            @Html.ActionLink("Details", "Details", new { id=item.car_cod }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.car_cod })*@
            <button onclick="location.href='@Url.Action("usuarioXCargo", "Persona", new {cargo =item.car_des})';return false;">Ver Usuarios</button>
        </td>
    </tr>
}

</table>
